<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:MauiAppMinhasCompras.Views"
       x:Class="MauiAppMinhasCompras.AppShell">

    <!-- Defina suas páginas de navegação -->
    <ShellContent Title="Novo Produto" ContentTemplate="{DataTemplate views:NovoProduto}" />
    <ShellContent Title="Lista de Produtos" ContentTemplate="{DataTemplate views:ListaProduto}" />
</Shell>
